pipeline {
    agent any

    stages {
         stage('cleanWs') {
            steps {
                echo 'cleanWS'
                //clean workspace before init
                cleanWs()
            }
        }
    }
}
